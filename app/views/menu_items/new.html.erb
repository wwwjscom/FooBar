<% semantic_form_for @menu_item do |f| %>
	<%= f.input :price, :label => 'Price', :hint => "Enter price in pennies" %>
	<%= f.input :name, :label => 'Name' %>
	<%= f.input :inventory_items, :as => :check_boxes, :label => 'Ingredients' %>
<% end %>